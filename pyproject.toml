[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fpl-skill"
version = "2.0.0"
description = "Autonomous MILP optimization engine and advisory agent skill for Fantasy Premier League"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
  "click>=8.0.0",
  "pulp>=2.7.0",
  "requests>=2.28.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=7.0.0",
  "pytest-cov>=4.0.0",
]

[project.scripts]
fpl = "fpl_skill.cli:cli"

[tool.setuptools.packages.find]
where = ["."]
include = ["fpl_skill*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
